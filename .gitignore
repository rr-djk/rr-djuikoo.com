# Dependencies
node_modules/

# Environment files
.env

# Terraform
# terraform.tfvars is committed on purpose (public repo, no secret values).
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
**/tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.tfstate.lock.info

# Agent build artefact
agent.zip

# Bedrock eval runs: one timestamped file per `make eval`, full of model output
# that changes every run. Keep one deliberately with `git add -f` to pin a
# baseline worth comparing against.
tests/results/
